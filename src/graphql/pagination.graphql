input PaginationParams {
  first: Int!
  after: String
}

enum SortDirection {
  asc
  desc
}

input SortParams {
  sortBy: String!
  sortDirection: SortDirection!
}

type PageInfo {
  endCursor: String
  hasNextPage: Boolean!
  firstCursor: String
  hasPrevPage: Boolean!
}
